const assert = require("assert");

describe("Given users api", function () {
  describe("when get all users", function () {
    it("should return list of users", function () {
      assert.strictEqual(-1, [1, 2, 3].indexOf(4));
    });
  });
});
